import React from "react";
import back from '../assets/img/back1.jpg';

const Header = () => {
    return <header>
        <img src={back} alt="manos programando" />
    </header>
}

export default Header;